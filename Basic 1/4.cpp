#include <bits/stdc++.h>
using namespace std;

struct pos {
    int x;
    int y;
};

void test() {
    vector<pos> t(3);
    for (auto &item:t) {
        cin >> item.x >> item.y;
    }

    //
}

int main() {
    int n;
    cin >> n;
    for (int i = 0; i < n; ++i) {
        test();
    }
    return 0;
}

#include <bits/stdc++.h>
using namespace std;

struct pos {
    int x;
    int y;
};

bool is_quad(pos a, pos b, pos c, pos d) {
    //
}

bool is_trape(pos a, pos b, pos c, pos d) {
    //
}

bool is_rhom(pos a, pos b, pos c, pos d) {
    //
}

bool is_sqr(pos a, pos b, pos c, pos d) {
    //
}

int main() {
    vector<pos> p(4);
    for (auto &item:p) cin >> item.x >> item.y;
}

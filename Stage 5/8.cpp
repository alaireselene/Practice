#include <bits/stdc++.h>
using namespace std;

void multiple(string &a, string b){
    vector <int> store;
    int alen = a.length(), blen = b.length();
    for (int k = blen - 1; k >= 0; --k){
        for (int i = alen - 1; i >= 0; --i){
            int temp = ((int) b[k]) * ((int) a[k]); 
        }
    }
}
int main(){
    // Code goes here;
}
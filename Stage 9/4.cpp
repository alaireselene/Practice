#include <bits/stdc++.h>
using namespace std;
typedef unsigned long int ui;

ui n;
vector<ui> in;

int main(){
    cin >> n;
    for (ui i = 0, x; i < n; ++i){
        cin >> x;
        in.push_back(x);
    }
}

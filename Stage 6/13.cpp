#include <bits/stdc++.h>
using namespace std;

vector <long long> a;
long long ans = INT32_MIN;

int n, pos;
// n - size of array.
// len - len of max.
// pos - pos of 1st position.

long long x;
// x - input.
// sum - answer.
// Never let sum = 0, if the answer is -1 lmao;

void inp(){
    cin >> n;
    for (int i = 0; i < n; ++i){
        cin >> x;
        a. push_back(x);
    }
}

void work(){
    int temp = a[0];
    for (int i = 1; i < n; ++i){
        temp = a[]
    }
}
int main(){
    inp();
    work();
    cout << ;
    return 0;
}
